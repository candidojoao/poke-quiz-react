import React from 'react'
import ReactDOM from 'react-dom/client'
import PokemonQuizPage from './PokemonQuizPage' 
import './index.css'
import './styles/sizes.css'
ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PokemonQuizPage />
  </React.StrictMode>,
)